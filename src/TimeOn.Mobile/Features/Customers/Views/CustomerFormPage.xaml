<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:TimeOn.Mobile.Features.Customers.ViewModels"
             x:Class="TimeOn.Mobile.Features.Customers.Views.CustomerFormPage"
             x:DataType="vm:CustomerFormViewModel"
             Title="{Binding Title}">

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="12">
            <ActivityIndicator IsRunning="{Binding IsLoading}" IsVisible="{Binding IsLoading}" />
            <Label Text="{Binding ErrorMessage}" Style="{StaticResource ErrorLabel}" />

            <Entry Placeholder="Name" Text="{Binding Name}" />
            <Entry Placeholder="Contact email" Text="{Binding ContactEmail}" />
            <Entry Placeholder="Address" Text="{Binding Address}" />

            <HorizontalStackLayout Spacing="8">
                <Label VerticalOptions="Center" Text="Active" />
                <Switch IsToggled="{Binding IsActive}" />
            </HorizontalStackLayout>

            <HorizontalStackLayout Spacing="8">
                <Button Text="Save" Command="{Binding SaveCommand}" />
                <Button Text="Cancel" Command="{Binding CancelCommand}" />
            </HorizontalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
