<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:TimeOn.Mobile.Features.Authentication.ViewModels"
             x:Class="TimeOn.Mobile.Features.Authentication.Views.LoginPage"
             x:DataType="vm:LoginViewModel"
             Title="Login">

    <VerticalStackLayout Padding="24" Spacing="12">
        <Entry Text="{Binding Email}" Placeholder="Email" Keyboard="Email" />
        <Entry Text="{Binding Password}" Placeholder="Password" IsPassword="True" />
        <Label Text="{Binding ErrorMessage}" Style="{StaticResource ErrorLabel}" />
        <Button Text="Sign in" Command="{Binding LoginCommand}" />
        <Button Text="Sign up" Command="{Binding GoToRegisterCommand}" />
        <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />
    </VerticalStackLayout>
</ContentPage>
